{
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.1.0",
    "prettier": "^3.1.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,md,html,css}": "prettier --write"
  },
  "scripts": {
    "postinstall": "husky install",
    "pre-commit": "lint-staged",
    "lint": "prettier --write -u ."
  }
}
